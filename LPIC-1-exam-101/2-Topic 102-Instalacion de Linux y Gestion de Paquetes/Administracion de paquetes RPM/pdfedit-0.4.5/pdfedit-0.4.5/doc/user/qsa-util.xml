<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.2//EN" "http://www.oasis-open.org/docbook/xml/4.2/docbookx.dtd">
<chapter id="p_QSA_Utility_Framework"><title>QSA Utility Framework</title>

The QSA Utility Framework is a set of classes that extends QSA to
enable the user to read and write files, access directory structures
and start processes synchronously and asynchronously.


<sect1 id="p_File_Class_Reference"><title>File Class Reference</title>

The <code>File</code> class provides functionallity for reading and writing
binary and text files. A <code>File</code> can be instantiated as an object,
giving the scripter complete flexibility when reading and writing files.
In addition, the <code>File</code> class provides a set of static convenence
functions for reading and writing files in one go.

Typical use of a <code>File</code> is:

<programlisting>// Reads an entire file in one go
var log = File.read('file.log');
// Writes an entire file in one go
File.write('copy_of_file.log', log);

// Read and write a file line by line
var infile = new File('file.log');
infile.open(File.ReadOnly);

var outfile = new File('copy_of_file.log');
outfile.open(File.WriteOnly);

while (!infile.eof) {
    var line = infile.readLine();
    outfile.write( line );
}

infile.close();
outfile.close();
</programlisting>
<sect2 id="p_Enums"><title>Enums</title>

<sect3 id="p_AccessMode"><title>AccessMode</title>

The enum <code>AccessMode</code> is used in conjunction with <code>File.open</code> to
specify the mode in which the file is opened.

<itemizedlist spacing="compact">
<listitem><para>ReadOnly; Opens the file in read-only mode. 

</para></listitem><listitem><para>WriteOnly; Opens the file in write-only mode. If this flag is
used with <code>Append,</code> the file is not truncated; but if used on its own
(or with <code>Truncate),</code> the file is truncated.

</para></listitem><listitem><para>ReadWrite; Opens the file in read/write mode. The file is <emphasis>not</emphasis>
truncated.

</para></listitem><listitem><para>Append; Opens the file in append mode. (You must actually use
<code>(WriteOnly | Append)</code> to make the file writable and to go into
append mode.) This mode is very useful when you want to write
something to a log file. The file index is set to the end of the file.
Note that the result is undefined if you position the file index
manually using at() in append mode.

</para></listitem><listitem><para>Truncate; Truncates the file. 

</para></listitem><listitem><para>Translate; Enables carriage returns and linefeed translation
for text files under Windows.

</para></listitem></itemizedlist>

</sect3></sect2><sect2 id="p_Static_Functions"><title>Static Functions</title>

<itemizedlist spacing="compact">
<listitem><para>exists( fileName : String ) : Boolean; Returns true if <code>fileName</code>
exists; otherwise returns false.

</para></listitem><listitem><para>remove( fileName : String ); Deletes the file <code>fileName</code> if
possible; otherwise throws an exception.

</para></listitem><listitem><para>write( fileName : String, content : String ); Writes the string 
content to the file <code>fileName</code> if possible (completely replacing the
original contents if the file already exists); otherwise throws an
exception.

</para></listitem><listitem><para>read( fileName : String ) : String; Reads and returns the contents of
the file <code>fileName</code> if possible; otherwise throws an exception.

</para></listitem><listitem><para>isFile( fileName : String ) : Boolean; Returns true if <code>fileName</code>
is a file; otherwise returns false.

</para></listitem><listitem><para>isDir( fileName : String ) : Boolean; Returns true if <code>fileName</code>
is a directory; otherwise returns false.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Constructor"><title>Constructor</title>

<itemizedlist spacing="compact">
<listitem><para>File( fileName : String ); Creates a file object with the with 
fileName. If <code>fileName</code> is missing or is not a String, an exception
is thrown.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Properties"><title>Properties</title>

The File object's properties are read-only.

<itemizedlist spacing="compact">
<listitem><para>name : String; The name of the file including the extension.

</para></listitem><listitem><para>path : String; The path of the file.

</para></listitem><listitem><para>fullName : String; The fullName of the file, including path, name, and
extension.

</para></listitem><listitem><para>baseName : String; The name of the file, excluding its path and
extension.

</para></listitem><listitem><para>extension : String; The file name's extension.

</para></listitem><listitem><para>symLink : String; The expansion of the symlink if the file is a
symlink; otherwise empty;

</para></listitem><listitem><para>exists : Boolean; True if the file exists; otherwise false.

</para></listitem><listitem><para>readable : Boolean; True if the file is readable; otherwise false.

</para></listitem><listitem><para>writable : Boolean; True if the file is writable; otherwise false.

</para></listitem><listitem><para>executable : Boolean; True if the file is executable; otherwise
false.

</para></listitem><listitem><para>hidden : Boolean; True if the file is hidden; otherwise false.

</para></listitem><listitem><para>eof : Boolean; True if reading has reached the end of the file;
otherwise false.

</para></listitem><listitem><para>created : Date; The creation time of the file.

</para></listitem><listitem><para>lastModified : Date; The last modification time of the file.

</para></listitem><listitem><para>lastRead : Date; The last time the file was read.

</para></listitem><listitem><para>size : Number; The size of the file, in bytes.

</para></listitem></itemizedlist>

</sect2><sect2 id="p_Member_Functions"><title>Member Functions</title>

<itemizedlist spacing="compact">
<listitem><para>open( accessMode : Number ); Opens the file in the mode 
accessMode if possible; otherwise throws an exception.

</para></listitem><listitem><para>close(); Closes the file.

</para></listitem><listitem><para>remove((); Deletes the file if possible; otherwise throws an
exception.

</para></listitem><listitem><para>readByte() : Number; Reads one byte from the file if possible;
otherwise throws an exception.

</para></listitem><listitem><para>read() : String; Returns the entire content of the file as a string
if it can be read; otherwise throws an exception.

</para></listitem><listitem><para>readLine() : String; Reads one line from file if possible;
otherwise throws an exception. Retains any trailing whitespace.

</para></listitem><listitem><para>readLines() : String[]; Returns the contents of the file as an
array of Strings, one for each line. Linebreaks are strippped from the
strings. If the file could not be read, an exception is thrown.

</para></listitem><listitem><para>writeByte( byte : Number ); Writes a byte to the file is possible;
otherwise throws an exception.

</para></listitem><listitem><para>write( data : String, length : Number ); Writes <code>length</code> number of
characters from <code>data</code> to the file if possible; otherwise throws an
exception.

</para></listitem><listitem><para>writeLine( data : String ); Writes the line <code>data</code> to the file and
adds a linebreak. If the file could not be written <code>error</code> is
returned.

</para></listitem></itemizedlist>


</sect2></sect1><sect1 id="p_Dir_Class_Reference"><title>Dir Class Reference</title>

The <code>Dir</code> class provides access to directory structures and their
contents in a platform-independent way. It provides a means of listing
directory content, creating filenames with proper path separators,
etc.

An example use of the Dir class is:

<programlisting>var dir = new Dir;
var codeFiles = dir.entryList('*.cpp');
for (var i = 0; i &lt; codeFiles.length; ++i) 
    print(codeFiles[i]);
</programlisting>
<sect2 id="p_Enums_0"><title>Enums</title>

<sect3 id="p_FilterSpec"><title>FilterSpec</title>

This enum describes the filtering options available to Dir for
entryList(). The filter value is specified by OR-ing together values
from the following list.

<itemizedlist spacing="compact">
<listitem><para>Dirs; List directories only.

</para></listitem><listitem><para>Files; List files only.

</para></listitem><listitem><para>Drives; List disk drives only.

</para></listitem><listitem><para>NoSymLinks; Do not list symbolic links (ignored by operating
systems that don't support symbolic links).

</para></listitem><listitem><para>All; List directories, files, drives and symlinks (this does not
list broken symlinks unless you specify System).

</para></listitem><listitem><para>TypeMask; A mask for the the Dirs, Files, Drives and NoSymLinks
flags.

</para></listitem><listitem><para>Readable; List files for which the application has read access. 

</para></listitem><listitem><para>Writable; List files for which the application has write access. 

</para></listitem><listitem><para>Executable; List files for which the application has execute
access. Executables must be combined with Dirs or Files.

</para></listitem><listitem><para>RWEMask; A mask for the Readable, Writable and Executable flags.

</para></listitem><listitem><para>Modified; Only list files that have been modified (ignored under
Unix).

</para></listitem><listitem><para>Hidden; List hidden files (on Unix, files starting with a .). 

</para></listitem><listitem><para>System; List system files (on Unix, FIFOs, sockets, and device
files)

</para></listitem><listitem><para>AccessMask; A mask for the Readable, Writable, Executable Modified,
Hidden and System flags

</para></listitem></itemizedlist>
</sect3><sect3 id="p_SortSpec"><title>SortSpec</title>

This enum describes the sort options available to Dir for
entryList(). The sort value is specified by OR-ing together values
from the following list:

<itemizedlist spacing="compact"><listitem><para>Name; Sort by name.
</para></listitem><listitem><para>Time; Sort by time (modification time).
</para></listitem><listitem><para>Size; Sort by file size.
</para></listitem><listitem><para>Unsorted; Do not sort.
</para></listitem><listitem><para>SortByMask; A Mask for Name, Time and Size.
</para></listitem><listitem><para>DirsFirst; Put the directories first, then the files.
</para></listitem><listitem><para>Reversed; Reverse the sort order.
</para></listitem><listitem><para>IgnoreCase; Sort case-insensitively.
</para></listitem></itemizedlist>
</sect3></sect2><sect2 id="p_Static_Properties"><title>Static Properties</title>

<itemizedlist spacing="compact"><listitem><para>current : String; The current directory.
</para></listitem><listitem><para>home : String; The home directory.
</para></listitem><listitem><para>root : String; The root directory.
</para></listitem><listitem><para>drives : String[]; An array of strings containing the drive names
(c:, d:, etc); empty on Unix.
</para></listitem></itemizedlist>
</sect2><sect2 id="p_Static_Functions_0"><title>Static Functions</title>

Qt uses "/" as a directory separator throughout (although it
understands the conventions of the platform it is used on). If you
are working with paths, use "/" within your code, and use
convertSeparators() when you want to display a path to the user.

<itemizedlist spacing="compact">
<listitem><para>cleanDirPath( filePath : String ) : String; Removes all multiple
directory separators "/" and resolves any "."s or ".."s found in the
path, <code>filePath.</code>

</para></listitem><listitem><para>convertSeparators( pathName : String ) : String; Returns 
pathName with the "/" separators converted to separators that are
appropriate for the underlying operating system.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Constructor_0"><title>Constructor</title>

<itemizedlist spacing="compact">
<listitem><para>Dir( path : String ); Creates a directory object for path 
path. If <code>path</code> is empty, the current directory is used.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Properties_0"><title>Properties</title>

<itemizedlist spacing="compact">
<listitem><para>name : String; Contains the name of the directory; this is not the
same as the path, e.g. a directory with the name "mail", might have
the path "/var/spool/mail"

</para></listitem><listitem><para>path : String; Contains the path, this may contain symbolic links,
but never contains redundant ".", "..", or multiple separators.

</para></listitem><listitem><para>absPath : String; Contains the absolute path (a path that starts
with "/" or with a drive specification), which may contain symbolic
links, but never contains redundant ".", "..", or multiple separators.

</para></listitem><listitem><para>canonicalPath : String; Contains the canonical path, i.e. a path
without symbolic links or redundant "." or ".." elements.

</para></listitem><listitem><para>readable : Boolean; True if the directory is readable; otherwise
false.

</para></listitem><listitem><para>exists : Boolean; True if the directory exists; otherwise false.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Member_Functions_0"><title>Member Functions</title>

<itemizedlist spacing="compact">
<listitem><para>filePath( fileName : String ) : String; Returns the path name of
the file <code>fileName</code> in the directory.

</para></listitem><listitem><para>absFilePath( fileName : String ) : String; Returns the absolute
path name of the file <code>fileName</code> in the directory.

</para></listitem><listitem><para>cd( dirName : String ); Changes the Dir's directory to <code>dirName</code>
if possible; otherwise throws an exception.
 
</para></listitem><listitem><para>cdUp(); Changes directory by moving one directory up from the
Dir's current directory if possible; otherwise throws an exception.

</para></listitem><listitem><para>entryList( filter : String, filterSpec : Number, sortSpec : Number
) : String[]; Returns a list of the names of all the files and
directories in the directory, ordered in accordance with <code>sortSpec</code>
and filtered in accordance with <code>filterSpec.</code>

</para></listitem><listitem><para>mkdir( dirName : String ); Creates the directory <code>dirName</code> if
possible; otherwise throws an exception.

</para></listitem><listitem><para>mkdir(); Creates this directory if possible possible; otherwise
throws an exception.

</para></listitem><listitem><para>mkdirs( dirName : String ); Creates the directory tree <code>dirName</code>
if possible; otherwise throws an exception.

</para></listitem><listitem><para>mkdirs(); Creates this directory tree if possible; otherwise throws
an exception.

</para></listitem><listitem><para>rmdir( dirName : String ); Deletes the directory <code>dirName</code> if
possible; otherwise throws an exception.

</para></listitem><listitem><para>rmdir(); Deletes this directory if possible; otherwise throws an
exception.

</para></listitem><listitem><para>rmdirs( dirName : String ); Deletes the directory structure 
dirName if possible; otherwise throws an exception.

</para></listitem><listitem><para>rmdirs(); Deletes this directory structure if possible; otherwise
throws an exception.

</para></listitem><listitem><para>fileExists( fileName : String ) : Boolean; Returns true if the file
<code>fileName</code> exists; otherwise returns false.

</para></listitem><listitem><para>setCurrent(); Sets the application's current working directory to
this directory if possible; otherwise throws an exception.

</para></listitem></itemizedlist>

</sect2></sect1><sect1 id="p_Process_Class_Reference"><title>Process Class Reference</title>

The Process class is used to start external programs and to
communicate with them. The Process class can start programs
synchronously using <code>Process.execute(),</code> and asynchronously using
<code>{new</code> Process(...).start()}.

<sect2 id="p_Static_Properties_0"><title>Static Properties</title>

<itemizedlist spacing="compact">
<listitem><para>stdout : String; Contains the data sent to <code>stdout</code> during the
last call to <code>Process.execute().</code> This property is read-only.

</para></listitem><listitem><para>stderr : String; Contains the data sent to <code>stderr</code> during the
last call to <code>Process.execute().</code> This property is read-only.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Static_Functions_1"><title>Static Functions</title>

<itemizedlist spacing="compact">
<listitem><para>execute( command : String, stdin : String ); Executes <code>command</code>
synchronously and passes <code>stdin</code> to its standard input if specified.
When the program ends its output is accessible through Process.stdout and <code>Process.stderr.</code> <code>command</code> can contain both the
program and command line arguments, e.g. <code> "ls -la" </code>. The function
returns the executed commands return code on exit.

</para></listitem><listitem><para>execute( command : String[], stdin : String ); Same as above,
except that <code>command</code> is an array of strings, where the first item is
the name of the program and the following items are command line
arguments. This version is useful if you have arguments that contain
spaces or other characters that would need to be quoted if you just
passed a single string command line, since it takes care of the
quoting for you. Note that if the user passes an array of only one
element this function behaves like the overload taking a string, thus
splitting it into "command" and "arguments".

\ executeNoSplit( command : String[], stdin : String ); Same as
above, except that <code>command</code> is an array of strings, where the first
item is the name of the program and the following items are command
line arguments. This version is useful if you have arguments that
contain spaces or other characters that would need to be quoted if you
just passed a single string command line, since it takes care of the
quoting for you. This function never splits the first argument,
regardless of the number of strings in the command array.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Constructor_1"><title>Constructor</title>

<itemizedlist spacing="compact">
<listitem><para>Process(); Creates a Process object without specifying the program
or any arguments. This does <emphasis>not</emphasis> start a process.

</para></listitem><listitem><para>Process( command : String ); Creates a Process object that will
execute <code>command.</code> This does <emphasis>not</emphasis> start the process.

</para></listitem><listitem><para>Process( command : String[] ); Same as above, except that 
command is an array of strings, where the first item is the name of
the program and the following items are command line arguments. This
version is useful if you have arguments that contain spaces or other
characters that would need to be quoted if you just passed a single
string command line, since it takes care of the quoting for you.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Properties_1"><title>Properties</title>

<itemizedlist spacing="compact">
<listitem><para>arguments : String[]; Contains an array of strings where the first
item is the program to execute and the following items are the
command line arguments.

</para></listitem><listitem><para>workingDirectory : String; Contains the working directory for the process.

</para></listitem><listitem><para>running : Boolean; True if the process is currently running;
otherwise false. This property is read-only.

</para></listitem><listitem><para>exitStatus : Number; Contains the exitStatus of the program when it
has finished. This property is read-only.

</para></listitem></itemizedlist>
</sect2><sect2 id="p_Member_Functions_1"><title>Member Functions</title>

<itemizedlist spacing="compact">
<listitem><para>start( env : String[] ); Tries to run a process for the command and
arguments that were specified with the <code>argument</code> property or that
were specified in the constructor. The command is searched for in the
path for executable programs; you can also use an absolute path in the
command itself. If <code>env</code> is not specified, the process is
started with the same environment as the starting process. If <code>env</code> is
specified, then the values in the <code>env</code> stringlist are interpreted as
environment setttings of the form <code>VARIABLE=VALUE</code> and the process is
started with these environment settings. If the program could not be
started, an exception is thrown.

</para></listitem><listitem><para>launch( stdin : String, env : String[] ); Runs the process and
writes the data <code>stdin</code> to the process's standard input. If all the
data is written to standard input, standard input is closed. The
command is searched for in the path for executable programs; you can
also use an absolute path in the command itself. If <code>env</code> is
unspecified, the process is started with the same environment as
the starting process. If <code>env</code> is specified, then the values in the
string list are interpreted as environment setttings of the form VARIABLE=VALUE and the process is started with these environment
settings. If the program could not be started, an exception is thrown.

</para></listitem><listitem><para>readStdout() : String; Reads what is currently available on the
process's standard output.

</para></listitem><listitem><para>readSterr() : String; Reads what is currently available on the
process's standard error.

</para></listitem><listitem><para>canReadLineStdout() : Boolean; Returns true if a line can be read
from the process's standard output.

</para></listitem><listitem><para>canReadLineStderr() : Boolean; Returns true if a line can be read
from the process's standard error.

</para></listitem><listitem><para>readLineStdout() : String; Reads one line of text from the
process's standard output if available; otherwise returns an empty
string.

</para></listitem><listitem><para>readLineStderr() : String; Reads one line of text from the
process's standard error if available; otherwise returns an empty
string.

</para></listitem><listitem><para>tryTerminate(); Asks the process to terminate. Processes can ignore
this if they wish. If you want to be certain that the process really
terminates, you can use <code>kill()</code> instead.

</para></listitem><listitem><para>kill(); Terminates the process. This is not a safe way to end a
process since the process will not be able to do any
cleanup. <code>tryTerminate()</code> is safer, but processes can ignore
<code>tryTerminate().</code>

</para></listitem><listitem><para>writeToStdin( buffer : String ); Writes the data <code>buffer</code> to the
process's standard input. The process may or may not read this data.

</para></listitem><listitem><para>closeStdin(); Closes the process's standard input.

</para></listitem></itemizedlist></sect2></sect1></chapter>